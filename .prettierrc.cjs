module.exports = {
  // 每行最大字符数
  printWidth: 100,
  
  // 使用2个空格缩进
  tabWidth: 2,
  
  // 使用空格而不是tab进行缩进
  useTabs: false,
  
  // 语句末尾不使用分号
  semi: false,
  
  // 使用单引号
  singleQuote: true,
  
  // 对象属性名在必要时才加引号
  quoteProps: 'as-needed',
  
  // JSX中使用双引号
  jsxSingleQuote: false,
  
  // 在多行逗号分隔的语法结构中，最后一行加尾逗号
  trailingComma: 'all',
  
  // 对象花括号内部放置空格
  bracketSpacing: true,
  
  // 将多行JSX元素的闭合标签放在最后一行的末尾，而不是单独一行
  bracketSameLine: false,
  
  // 箭头函数唯一参数是否使用括号
  arrowParens: 'always',
  
  // 格式化文件的起止范围
  rangeStart: 0,
  rangeEnd: Infinity,
  
  // 不要在文件顶部插入pragma标记
  requirePragma: false,
  insertPragma: false,
  
  // Markdown文本换行方式
  proseWrap: 'preserve',
  
  // HTML空白处理方式
  htmlWhitespaceSensitivity: 'css',
  
  // Vue文件中的<script>和<style>标签内的代码是否缩进
  vueIndentScriptAndStyle: false,
  
  // 换行符
  endOfLine: 'lf',
  
  // 格式化嵌入式内容，如markdown中的代码块
  embeddedLanguageFormatting: 'auto',
  
  // 格式化HTML中的空白
  htmlWhitespaceSensitivity: 'strict',
} 